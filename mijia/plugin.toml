[plugin]
id = "mijia"
name = "米家智能设备插件"
description = "控制米家智能设备，支持设备发现、属性读写、动作调用"
version = "1.0.0"
entry = "plugins.mijia:MijiaPlugin"

[plugin.sdk]
recommended = ">=0.1.0,<0.2.0"
supported = ">=0.1.0,<0.3.0"

[plugin.author]
name = "N.E.K.O Team"
email = ""

[plugin.dependencies]
# 插件依赖的Python包
packages = [
    "pillow",        # 已存在
    "qrcode",        # 已存在
    "requests",      # 已存在
]